import React from "react";
import Index from "./Index";

const Home = () => {
    // A página inicial renderiza diretamente a página principal de Vendas.
    // O Quiz ainda pode ser acessado em /quiz caso deseje criar um funil separado no futuro.
    return <Index />;
};

export default Home;
